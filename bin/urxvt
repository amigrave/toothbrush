#!/bin/bash
num=`ps aux | grep "/usr/bin/urxvt" | wc -l`
/usr/bin/urxvt -icon ~/.agr/icons/terminal${num}.png "$@"

#urxvtc "$@"
#if [ $? -eq 2 ]; then
#	urxvtd -q -o -f
#	urxvtc -icon ~/.terminal.png "$@"
#fi
