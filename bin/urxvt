#!/bin/bash
num=`ps aux | grep "/usr/bin/urxvt" | grep -v grep | wc -l`
num=`expr $num + 1`
/usr/bin/urxvtc -icon ~/.agr/icons/terminal${num}.png "$@"

#urxvtc "$@"
#if [ $? -eq 2 ]; then
#	urxvtd -q -o -f
#	urxvtc -icon ~/.terminal.png "$@"
#fi
